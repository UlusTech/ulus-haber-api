{
  "version": "0.1.0",
  "scripts": {
    "test": "bun test",
    "dev": "bun run --watch src/main.ts",
    "setup:pg": "bunx drizzle-kit generate && bun run src/migrate.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.6",
    "elysia": "latest"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "bun-types": "latest",
    "drizzle-kit": "^0.31.5"
  },
  "license": "MIT",
  "module": "src/main.js"
}
